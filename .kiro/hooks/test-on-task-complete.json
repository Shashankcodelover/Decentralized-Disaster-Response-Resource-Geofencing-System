{
  "name": "Run Tests After Task",
  "version": "1.0.0",
  "description": "Run the full test suite after a spec task is marked complete",
  "when": {
    "type": "postTaskExecution"
  },
  "then": {
    "type": "runCommand",
    "command": "npm run test"
  }
}
